<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>EntomologyDataCollect</title>
    <link rel="stylesheet" type="text/css" href="css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <h1>Entomology DataCollect</h1>
    <div class="container">
      <fieldset>
        <form action="/downloadCSV" method="get">
            <input name="formType" value=<%= formType %> readonly>
            <select class="selectpicker" data-style="btn-info" name="projectCode">
                <optgroup label="Select Table">
                    <option name="" value="null">Select Form Type</option>
                    <% for(var i=0;i<projectCodes.length;i++){ %>
                    <option name="<%= projectCodes[i] %>"  value="<%=projectCodes[i] %>"><%= projectCodes[i] %></option>
                    <% } %>
                </optgroup>
            </select>
            <input type='submit'name ='register' value='Download'>
      </fieldset>
    </div>
  </body>
</html>
